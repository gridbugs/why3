#!/bin/sh

config=src/util/config.ml
makefileconfig=share/Makefile.config

cat - > $config <<EOF
let version   = "@VERSION@"

let localdir  = Sites_info.sourceroot
let libdir    = Sites_info.Sites.libdir
let datadir   = Sites_info.Sites.datadir
EOF

echo "
INFERLIB    = @INFERLIB@
" > $makefileconfig
